[project]
name = "whoop-connector"
version = "0.1.0"
description = "WHOOP API connector with MCP server and dashboard integration"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "mcp[cli]>=1.0",
    "python-dotenv>=1.0",
]

[project.scripts]
whoop-mcp = "whoop_mcp_server:main"
